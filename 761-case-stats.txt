requirements

- use BluePrintJS DateRangePicker
- use Mapbox to indicate view by country
- show the number of cases in the library
- enabled i18n for the entire page
- add a menu link to the stats page from the case
- add a policy so only super_admins, editors, library_managers, deployers can view the stats
- add a stats_query_builder_service using raw sql
- ensure the stats incliudes all the different slugs for the csae
- create an AGENTS.md file for opencode to use
- show use query params to mody the query
- add a JSON endpoint for stats
- add a service that returns GeoJson data or possibly MVT vector tiles
- hook the query service up the redux store (maybe)
- ensure their are multiple exports for the stats (tabular and PDF)
- add canned qurries as a yaml file
- create a websockt stream to see stats in realtime when viewers are looking at the case
- use React for a multi state view
- create a better scale for colors and enable Mapbox interctivity