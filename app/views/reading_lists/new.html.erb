<% content_for(:title) { I18n.t 'reading_lists.new.new_list' } %>

<div class="window admin reading-list pt-dark bp4-dark">
  <h1 class="admin__title">
    <%= t '.new_list' %>
  </h1>

  <%= render 'form', reading_list: @reading_list %>
</div>
