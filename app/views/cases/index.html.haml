= render "admin_cases_menu"

%main.admin-board
  %h2
    All Cases

  %table
    %thead
      %td.admin-case-slug Slug
      %td
      %td
      %td Catalog Position
      %td Published
      -#%td Category
      %td
        = button_to "New Case", new_case_path, class: "button--subtle button--subtle--reverse",
          method: :get
    %tbody
      -@cases.each do |kase|
        %tr
          %td.admin-case-slug=kase.slug
          %td
            %img.admin-case-cover-thumbnail{src: ix_cover_image(kase, :small)}
          %td=kase.kicker.upcase
          %td=kase.catalog_position.humanize.titlecase
          %td{class: "admin-case-date#{kase.published_at.nil? ? '--forthcoming' : '--published'}"}
            =kase.published_at || "Forthcoming"
          -#%td
          %td
            = button_to "Edit Case", case_path(kase, anchor: "/edit"),
              method: :get,
              class: "button--subtle"
