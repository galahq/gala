Hello <%= @reader.initials %>,

You were just enrolled in a new Michigan Sustainability Case.

**<%= @case.kicker.gsub(/\n/, '').titlecase %> — <%= @case.title.gsub(/\n/, '') %>**

*<%= @case.dek.gsub(/\n/, '') %>*

<%= @case.summary %>

<% if @token %>
[Create a password](<%= edit_password_url(@reader, reset_password_token: @token) %>) to read the case 
<% else %>
[Read the case](<%= case_url(@reader.locale, @case.slug) %>) 
<% end %>
on the Michigan Sustainability Cases website.
