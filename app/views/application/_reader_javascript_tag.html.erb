<%= javascript_tag do %>
  window.reader = <% if reader_signed_in? %>
  <%= raw ActiveModel::Serializer.for(
        current_reader.decorate, view_context: controller.view_context
      ).to_json %>
  <% else %>
  undefined;
  <% end %>
<% end %>
