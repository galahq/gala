<% content_for :title do %>
  <%= I18n.t('deployments.edit.edit_embedded_assessment') %>
  —
  <%= deployment.case.short_title %>
<% end %>
<% if show_header? %>
  <div class="admin">
    <div class="pt-dark bp4-dark">
      <h1 class="admin__title"><%= t '.edit_embedded_assessment' %></h1>
    </div>
  </div>
<% end %>
<div id="deployment-app" data-params="<%= render(template: 'deployments/edit',
formats: [:json]) %>" style="width: 100%"></div>
