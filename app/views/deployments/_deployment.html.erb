<div
  class="deployment spaced--compact"
  data-gala-controller="deployments--invite-drawer anchor-focus"
  data-gala-target="deployments--invite-drawer.container anchor-focus.container"
  id="d<%= deployment.id %>"
>
  <div class="deployment__details row--compact">
    <div class="deployment__case">
      <span class="deployment__case__kicker">
        <%= deployment.case.kicker %>
      </span>

      <%= spotlight :deployment_details, placement: 'top' do %>
        <%= link_to(
              deployment,
              class: %w[pt-button bp4-button pt-small bp4-small pt-minimal bp4-minimal pt-icon-people bp4-icon-people]
            ) do %>
          <%= t '.n_enrolled',
                count: reader_counts_by_deployment_id[deployment.id]
          %>
        <% end %>
      <% end %>
    </div>

    <div class="deployment__actions">
      <%= deployment.quiz_link %>

      <%= spotlight :invite_learners, placement: 'top' do %>
        <button
          class="deployment__open-invite pt-button bp4-button pt-small bp4-small
                 pt-minimal bp4-minimal pt-icon-new-person bp4-icon-new-person"
          data-gala-action="deployments--invite-drawer#open"
        >
          <%= t '.invite_learners' %>
        </button>
      <% end %>

      <button
        class="deployment__close-invite pt-button bp4-button pt-small bp4-small
               pt-minimal bp4-minimal pt-icon-cross bp4-icon-cross"
        data-gala-action="deployments--invite-drawer#close"
      >
        <%= t 'helpers.close' %>
      </button>
    </div>
  </div>

  <%= render 'invite_drawer', deployment: deployment %>
</div>
