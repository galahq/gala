<% headline t '.subject' %>

<%= t '.greeting', recipient: @email %>

<%= t '.message' %>

<%= t '.instruction' %>

<%= md_button_to t('.action'),
                 unlock_url(@resource, unlock_token: @token) %>
