<nav class="Toolbar__bar pt-dark bp4-dark">
  <div class="MaxWidthContainer">
    <div class="Toolbar__group pt-navbar-group bp4-navbar-group">
      <%= link_to t('catalog.catalog'), root_path,
                  class: 'Toolbar__item pt-button bp4-button pt-minimal bp4-minimal pt-icon-home' %>
      <%= link_to my_cases_nav_button_text, my_cases_path,
                  class: 'Toolbar__item pt-button bp4-button pt-minimal bp4-minimal pt-icon-annotation' %>
      <%= link_to deployments_nav_button_text, deployments_path,
                  class: 'Toolbar__item pt-button bp4-button pt-minimal bp4-minimal pt-icon-follower' %>
    </div>

    <div class="Toolbar__group pt-navbar-group bp4-navbar-group"></div>

    <div class="Toolbar__group pt-navbar-group bp4-navbar-group">
      <%= form_with url: '/catalog/search', local: true, method: :get do %>
        <div class="Toolbar__search pt-input-group bp4-input-group pt-round bp4-round">
          <span class="pt-icon bp4-icon pt-icon-search"></span>
          <input class="pt-input bp4-input" name="q" role="search" aria-label="Search Cases" placeholder="<%= I18n.t('search.search_cases') %>" type="text">
            <span class="pt-input-action bp4-input-action">
              <button type="submit" aria-label="Submit search" class="Toolbar__item Toolbar__search-action pt-button bp4-button pt-minimal bp4-minimal pt-icon-arrow-right"></button>
            </span>
          </input>
        </div>
      <% end %>
    </div>
  </div>
</nav>

<div class="window window-admin">
  <div class="MaxWidthContainer">
    <%= yield %>
  </div>
</div>

<% parent_layout 'with_header' %>
