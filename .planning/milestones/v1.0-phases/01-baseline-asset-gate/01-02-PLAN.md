---
phase: 01-baseline-asset-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/javascript/shared/blueprintLegacyNamespace.js
  - app/javascript/shared/__tests__/blueprintLegacyNamespace.test.js
autonomous: true
requirements:
  - FOUND-03
  - QA-03
must_haves:
  truths:
    - "Runtime elements with legacy `.pt-*` classes receive missing `.bp4-*` equivalents."
    - "Runtime namespace bridging never removes, replaces, or rewrites existing `.pt-*` classes."
    - "Rails-rendered legacy exclusions remain excluded in Phase 1."
    - "Focused Jest coverage proves the bridge handles existing DOM nodes, added nodes, and class mutations."
  artifacts:
    - path: "app/javascript/shared/blueprintLegacyNamespace.js"
      provides: "Runtime legacy Blueprint namespace bridge"
      contains: "const LEGACY_NAMESPACE = 'pt-'"
    - path: "app/javascript/shared/__tests__/blueprintLegacyNamespace.test.js"
      provides: "Regression tests for namespace bridge behavior"
  key_links:
    - from: "app/javascript/packs/styles.js"
      to: "app/javascript/shared/blueprintLegacyNamespace.js"
      via: "global pack import"
      pattern: "shared/blueprintLegacyNamespace"
    - from: "app/javascript/shared/blueprintLegacyNamespace.js"
      to: "document.body"
      via: "DOMContentLoaded/startup bridge"
      pattern: "document\\.addEventListener\\('DOMContentLoaded'"
---

<objective>
Lock down the Blueprint legacy namespace bridge before route-specific QA begins.

Purpose: The app still contains many hand-authored `.pt-*` classes while current Blueprint packages emit `.bp4-*` styles. Per D-01, keep the broad compatibility bridge during route stabilization. Per D-02, keep current Rails-rendered exclusions. Per D-03, only add missing `.bp4-*` equivalents. Per D-04, defer selector cleanup to Phase 9.

Output: Focused Jest coverage for the runtime bridge plus any narrow implementation adjustment needed for those locked decisions.
</objective>

<execution_context>
@/Users/nathanpapes/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nathanpapes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-asset-gate/01-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@app/javascript/packs/styles.js
@app/javascript/shared/blueprint.scss
@app/javascript/shared/blueprintLegacyNamespace.js

<interfaces>
From `app/javascript/shared/blueprintLegacyNamespace.js`:
```javascript
const LEGACY_NAMESPACE = 'pt-'
const BLUEPRINT_NAMESPACE = 'bp4-'
const RAILS_RENDERED_LEGACY_SELECTOR = '.Toolbar__bar, .window-admin, .window.admin'

function isRailsRenderedLegacyElement(node)
function mirrorLegacyClasses(node)
function mirrorLegacyClassesIn(root)
function observeLegacyClassChanges()
function startLegacyClassBridge()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add namespace bridge behavior tests</name>
  <files>app/javascript/shared/__tests__/blueprintLegacyNamespace.test.js, app/javascript/shared/blueprintLegacyNamespace.js</files>
  <behavior>
    - Test 1: An existing `<button class="pt-button pt-intent-primary">` keeps both `pt-*` classes and gains `bp4-button bp4-intent-primary`.
    - Test 2: A node that already has the matching `bp4-*` class does not receive duplicates.
    - Test 3: Elements inside `.Toolbar__bar`, `.window-admin`, and `.window.admin` keep their `pt-*` classes but do not gain mirrored `bp4-*` classes per D-02.
    - Test 4: A node appended after startup receives mirrored classes through `MutationObserver`.
    - Test 5: A class added after startup receives its missing `bp4-*` equivalent through the attributes observer.
  </behavior>
  <action>Create `app/javascript/shared/__tests__/blueprintLegacyNamespace.test.js` using Jest's jsdom environment. Structure the test so each case resets `document.body` and reloads the module with `jest.resetModules()` to trigger startup behavior. If direct testing is blocked by private helper scope, export named helpers from `blueprintLegacyNamespace.js` while preserving current side-effect startup import behavior for `styles.js`. Keep the broad bridge per D-01 and the exclusions per D-02.</action>
  <verify>
    <automated>yarn test app/javascript/shared/__tests__/blueprintLegacyNamespace.test.js --runInBand</automated>
  </verify>
  <done>The bridge behavior is covered by focused tests for existing DOM, newly added DOM, class mutations, duplicate prevention, and Rails-rendered legacy exclusions.</done>
</task>

<task type="auto">
  <name>Task 2: Correct bridge behavior only where tests prove drift</name>
  <files>app/javascript/shared/blueprintLegacyNamespace.js</files>
  <action>If the new tests fail, adjust `blueprintLegacyNamespace.js` narrowly so it only adds missing `.bp4-*` equivalents and never removes or rewrites `.pt-*` classes per D-03. Preserve `.Toolbar__bar`, `.window-admin`, and `.window.admin` exclusions per D-02. Do not migrate `.pt-*` selectors in `app/javascript/shared/blueprint.scss`; that cleanup is deferred by D-04.</action>
  <verify>
    <automated>yarn test app/javascript/shared/__tests__/blueprintLegacyNamespace.test.js --runInBand</automated>
  </verify>
  <done>Runtime namespace bridging matches the locked Phase 1 decisions and remains compatible with `styles.js` side-effect import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime DOM -> compatibility bridge | Rails and React render DOM nodes that the bridge mutates by adding compatibility classes. |
| Route content -> global script | Every route receives the bridge through the global styles pack. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | `blueprintLegacyNamespace.js` DOM mutation | mitigate | Tests verify the bridge only adds missing `.bp4-*` classes and preserves original `.pt-*` classes per D-03. |
| T-01-02-02 | Denial of Service | `MutationObserver` bridge | mitigate | Tests cover added nodes and class mutation behavior without recursive duplicate class growth. |
| T-01-02-03 | Elevation of Privilege | Global bridge on admin/auth surfaces | accept | The bridge changes CSS classes only and does not alter authorization state; route-specific admin/auth checks occur in later phases. |
</threat_model>

<verification>
Run `yarn test app/javascript/shared/__tests__/blueprintLegacyNamespace.test.js --runInBand`.
</verification>

<success_criteria>
FOUND-03 is satisfied when the legacy namespace bridge is tested and preserves the Phase 1 compatibility contract: `.pt-*` stays in place, missing `.bp4-*` classes are added, and current exclusions remain untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-asset-gate/01-baseline-asset-gate-02-SUMMARY.md`.
</output>
