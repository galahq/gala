---
phase: 01-baseline-asset-gate
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .planning/phases/01-baseline-asset-gate/01-QA-GATE.md
autonomous: false
requirements:
  - FOUND-01
  - FOUND-04
  - QA-01
  - QA-02
  - QA-03
  - QA-04
must_haves:
  truths:
    - "Developer can connect to the Rails app at http://localhost:3000 or has a documented startup blocker."
    - "Phase 1 has a route checklist derived from config/routes.rb and the locked baseline sample."
    - "Representative browser QA records page result, console status, network status, screenshots when useful, tests run, and commit hash field."
    - "Any new console error, failed pack load, or unexpected failed request blocks Phase 1 instead of being marked complete."
  artifacts:
    - path: ".planning/phases/01-baseline-asset-gate/01-QA-GATE.md"
      provides: "Repeatable QA gate evidence format and Phase 1 baseline results"
      contains: "Route Checklist"
    - path: "config/routes.rb"
      provides: "Source of truth for route groups and representative route selection"
      contains: "root to: 'catalog#home'"
  key_links:
    - from: ".planning/phases/01-baseline-asset-gate/01-QA-GATE.md"
      to: "config/routes.rb"
      via: "route checklist derived from Rails routes"
      pattern: "Source: `config/routes.rb`"
    - from: ".planning/phases/01-baseline-asset-gate/01-QA-GATE.md"
      to: "http://localhost:3000"
      via: "browser QA evidence"
      pattern: "localhost:3000"
---

<objective>
Create and execute the repeatable Phase 1 QA gate.

Purpose: Later phases need the same evidence shape before commits. Per D-09 and D-10, record route checklist, browser result, console/network status, screenshots when useful, targeted test commands/results, and commit hash under the phase directory. Per D-11, new console errors, failed pack loads, and unexpected failed requests block completion. Per D-13 through D-16, verify `/`, `/up`, one known case route if data exists, and one auth/admin route if reachable, documenting substitutions when data or credentials are unavailable.

Output: `.planning/phases/01-baseline-asset-gate/01-QA-GATE.md` populated with Phase 1 evidence and reusable fields for later phases.
</objective>

<execution_context>
@/Users/nathanpapes/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nathanpapes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-asset-gate/01-CONTEXT.md
@.planning/codebase/TESTING.md
@config/routes.rb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 1 QA gate artifact</name>
  <files>.planning/phases/01-baseline-asset-gate/01-QA-GATE.md</files>
  <action>Create `.planning/phases/01-baseline-asset-gate/01-QA-GATE.md` with sections for: phase, date, source route file, route checklist, representative URLs, browser result, console issues, network issues, screenshots, automated tests, substitutions, blocking issues, non-blocking pre-existing noise, and commit hash. Include the Phase 1 route sample per D-13: `/`, `/up`, one known case route if data exists, and one auth/admin route if reachable. Add fields to document missing data or credentials and closest substitute per D-14. Keep this artifact small and directly reusable in later phases per D-10.</action>
  <verify>
    <automated>test -f .planning/phases/01-baseline-asset-gate/01-QA-GATE.md</automated>
    <automated>rg "Route Checklist|Representative URLs|Console Issues|Network Issues|Automated Tests|Commit Hash" .planning/phases/01-baseline-asset-gate/01-QA-GATE.md</automated>
  </verify>
  <done>The QA artifact exists under the phase directory and contains every required gate field from D-09 through D-15.</done>
</task>

<task type="auto">
  <name>Task 2: Verify localhost startup and targeted automated checks</name>
  <files>.planning/phases/01-baseline-asset-gate/01-QA-GATE.md</files>
  <action>Check whether `http://localhost:3000/up` responds. If the Rails app is not running, try the repository's normal local startup path per D-16; request permission if sandbox, network, or long-running process constraints require it. Record the exact startup command, result, and any blocker in the QA gate. Run the targeted tests from plans 01 and 02, plus `bundle exec rspec spec/requests/health_check_spec.rb` for `/up`. Per D-12, run broader `yarn test` or broader RSpec only if shared/global changes were substantial. Record commands and pass/fail results.</action>
  <verify>
    <automated>curl -fsS http://localhost:3000/up</automated>
    <automated>yarn test app/javascript/shared/__tests__/blueprintAssetContract.test.js app/javascript/shared/__tests__/blueprintLegacyNamespace.test.js --runInBand</automated>
    <automated>bundle exec rspec spec/requests/health_check_spec.rb</automated>
  </verify>
  <done>`localhost:3000` is reachable or startup blockers are documented, targeted automated checks are recorded, and `/up` behavior is verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser QA gate for baseline routes</name>
  <what-built>Plans 01 and 02 established the asset and namespace baseline; Tasks 1 and 2 created the QA evidence artifact and verified automated checks.</what-built>
  <how-to-verify>
    1. Visit `http://localhost:3000/` in a browser.
    2. Confirm the catalog page renders with Blueprint controls styled and no obvious duplicate/missing Blueprint styles.
    3. Check browser console for new errors and network tab for failed pack, CSS, font, or unexpected request failures.
    4. Visit `http://localhost:3000/up` and confirm plain `OK`.
    5. Visit one known case route if data exists; if not, record the missing slug/data and substitute the closest reachable route.
    6. Visit one auth/admin route if reachable; if credentials are unavailable, record that and substitute the sign-in route or closest reachable protected route.
    7. Add screenshots to the QA artifact when visual judgment is ambiguous per D-15.
    8. Fill the commit hash field after the phase commit per QA-04.
  </how-to-verify>
  <resume-signal>Type "approved" if browser QA matches the expected baseline, or describe the blocking console/network/visual issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser QA -> local Rails app | The browser requests public, authenticated, and static asset routes from localhost. |
| QA evidence -> future phase decisions | The phase note becomes the gate used to decide whether later route phases may advance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Repudiation | `01-QA-GATE.md` | mitigate | Require route checklist, browser result, console/network status, automated test commands/results, substitutions, blockers, and commit hash per D-09 and FOUND-04. |
| T-01-03-02 | Denial of Service | Local app startup | mitigate | Verify `curl -fsS http://localhost:3000/up` and document startup blockers per D-16 instead of silently advancing. |
| T-01-03-03 | Information Disclosure | Screenshots / QA notes | mitigate | Store only local QA evidence and avoid secrets, credentials, cookies, or private tokens in screenshots and notes. |
</threat_model>

<verification>
Overall Phase 1 checks:
- `curl -fsS http://localhost:3000/up`
- `yarn test app/javascript/shared/__tests__/blueprintAssetContract.test.js app/javascript/shared/__tests__/blueprintLegacyNamespace.test.js --runInBand`
- `bundle exec rspec spec/requests/health_check_spec.rb`
- Browser QA of `/`, `/up`, one known case route if data exists, and one auth/admin route if reachable, with console/network inspection recorded in `.planning/phases/01-baseline-asset-gate/01-QA-GATE.md`.
</verification>

<success_criteria>
FOUND-01, FOUND-04, QA-01, QA-02, QA-03, and QA-04 are satisfied when the Rails app is reachable or blocked with evidence, the Phase 1 route checklist and browser QA are recorded, targeted automated checks are listed with results, and the phase is committed only after the gate passes or has documented non-blocking exceptions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-asset-gate/01-baseline-asset-gate-03-SUMMARY.md`.
</output>
