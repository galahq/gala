---
milestone: v1.1
status: tech_debt
audited_at: 2026-06-01T22:29:36Z
result: phase_closeout_complete_with_ci_debt
---

# v1.1 Milestone Audit

## Result

The v1.1 phase closeout is complete enough to commit the dirty GSD/cache/deploy artifacts, but the milestone is not ready to archive as clean.

## Closed In This Audit

- Phase 16 through Phase 29 roadmap status was reconciled against completed artifacts on disk.
- Phase 30 was added to capture the dirty closeout, focused cache verification, GitHub/AWS guardrail checks, and milestone audit debt.
- Phase 28 ARM64 and Thruster decisions were superseded by Phase 32:
  - ARM64 production adoption is accepted for the greenfield AWS environment; production-capable default is `arm64`.
  - Thruster remains no-adopt.
- Phase 24 cache/test-harness follow-up was closed by focused request-spec validation.

## Passed Evidence

- `./run-rspec.sh spec/requests/catalog_routes_spec.rb spec/requests/case_routes_spec.rb` passed with 29 examples and 0 failures.
- `pnpm test -- --runInBand app/javascript/catalog/__tests__/readerData.test.js` passed with 103 tests passed and 3 skipped.
- Workflow YAML parse, deploy script syntax, operator script syntax, operator docs validation, CI script tests, and SST TypeScript no-emit checks passed.
- Read-only AWS CLI inspection confirmed the expected account, deployed CloudFront distributions, and ECS dev services running 1/1.
- Public health checks returned HTTP 200 for `https://dev.learngala.dev/up` and `https://learngala.dev/up`.

## Tech Debt Blocking Clean Archive

- The latest inspected remote `gala/ci-validation` status remained `failure` on `e2445ae9`.
- The downloaded validation artifact for run `26755143627` reported `state: failure`, confidence 35, with failed dimensions for integration, Ruby lint, ESLint, and Stylelint.
- Broad lint commands still fail on legacy debt outside this closeout scope:
  - `bundle exec rubocop --format simple`
  - `pnpm exec eslint app/javascript`
  - `pnpm exec stylelint "app/assets/stylesheets/**/*.scss" "app/assets/stylesheets/**/*.css"`
- The exact closeout commit must be pushed and its GitHub validation artifact inspected before the milestone archive workflow is allowed.

## Non-Mutation Statement

This audit did not touch Heroku production, `.com` DNS, SES, or retained media buckets. AWS checks were read-only except for any separately triggered GitHub preview deploy workflow, which targets the SST-managed preview/dev environment rather than Heroku production.
