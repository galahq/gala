---
phase: 16
plan: 1
type: execute
title: Implement reliable pnpm-backed frontend test runner contract
status: completed
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - jest.config.js
  - spec/support/jest-setup.js
  - .planning/phases/16-frontend-test-runner-modernization/16-01-SUMMARY.md
autonomous: true
requirements:
  - VITE-02
  - VITE-03
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - QA-04
must_haves:
  truths:
    - "There is one reproducible, single-command frontend test entrypoint in package.json."
    - "RUNNER_COMMAND evidence shows `pnpm` executes the chosen frontend runner with explicit env setup."
    - "Representative frontend suites are run through the phase-selected runner and recorded as pass/fail."
    - "If Vitest is not fully feasible, VITE-03 fallback path is explicit and executable."
  artifacts:
    - path: "package.json"
      provides: "Single-command frontend test script + optional explicit fallback runner scripts"
      contains: "Node env + pnpm-backed test command"
    - path: ".planning/phases/16-frontend-test-runner-modernization/16-01-SUMMARY.md"
      provides: "Runner evidence and compatibility decision"
      contains: "VITE-02 / VITE-03 outcome and gate notes"
    - path: "jest.config.js"
      provides: "Jest transform and setup contract used by TEST-01/TEST-02"
      contains: "modulePaths/module extensions/transform/setup"
  key_links:
    - from: "package.json"
      to: ".planning/phases/16-frontend-test-runner-modernization/16-RESEARCH.md"
      via: "runner decision gating"
      pattern: "test command modernization"
    - from: ".planning/phases/12-vitest-and-vite-feasibility/12-01-SUMMARY.md"
      to: ".planning/phases/16-frontend-test-runner-modernization/16-01-PLAN.md"
      via: "runner feasibility and fallback rationale"
      pattern: "Vitest blockers / Jest fallback"

<objective>
Deliver a single reliable frontend test command for phase-16 scope using pnpm, while explicitly confirming the active runner choice from Phase 12 feasibility evidence.
</objective>

<execution_context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-frontend-test-runner-modernization/16-CONTEXT.md
@.planning/phases/16-frontend-test-runner-modernization/16-RESEARCH.md
@.planning/phases/12-vitest-and-vite-feasibility/12-01-SUMMARY.md
@package.json
@jest.config.js
@spec/support/jest-setup.js
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm runner baseline and record phase decision evidence</name>
  <read_first>
    .planning/phases/16-frontend-test-runner-modernization/16-RESEARCH.md
    .planning/phases/12-vitest-and-vite-feasibility/12-01-SUMMARY.md
    .planning/REQUIREMENTS.md
    package.json
    jest.config.js
    spec/support/jest-setup.js
    .planning/STATE.md
  </read_first>
  <action>
    Reconfirm current blockers and compatibility data that determine runner choice:

    - validate existing test baseline:
      - `pnpm test -- --runInBand` command shape and exit status,
      - representative commandability under pnpm for targeted tests,
      - current `jest` API usage in test surfaces.
    - classify the active runner choice for this phase using `12-01-SUMMARY.md`:
      - Vitest can proceed only if representative test migration risk is removed,
      - otherwise explicitly choose Jest fallback and mark `VITE-03` as the active lane.
    - create the initial runner decision section in `16-01-SUMMARY.md` with this baseline evidence.
  </action>
  <acceptance_criteria>
    - Baseline verification command outputs are captured before any test runner migration changes.
    - `16-RESEARCH.md` plus phase 12 evidence are linked to an explicit `VITE-02` decision or `VITE-03` fallback lane.
  </acceptance_criteria>
  <verify>
    pnpm test -- --runInBand
    pnpm test -- --runInBand app/javascript/shared/__tests__/functions.test.js
    rg -n "describe\\(|test\\(|jest\\.|vitest" app/javascript spec/support jest.config.js
    rg -n "VITE-02|VITE-03|Vitest|jest" .planning/phases/16-frontend-test-runner-modernization/16-01-SUMMARY.md .planning/phases/12-vitest-and-vite-feasibility/12-01-SUMMARY.md
  </verify>
</task>

<task type="auto">
  <name>Task 2: Standardize the single pnpm-backed frontend test entry command</name>
  <read_first>
    package.json
    .planning/phases/16-frontend-test-runner-modernization/16-RESEARCH.md
    .planning/phases/12-vitest-and-vite-feasibility/12-01-SUMMARY.md
  </read_first>
  <action>
    Update `package.json` scripts so there is one primary pnpm test entrypoint for frontend tests.
    Suggested pattern:
    - keep `test` as the stable command,
    - add `test:frontend` or `test:ci` for explicit phase selection if needed.

    Decision branch:
    - If Task 1 confirms Vitest is feasible under bounded migration scope: point the primary script to the Vitest command and run with selected include list.
    - If not feasible (expected on first pass): pin primary runner to Jest with explicit `NODE_ENV=test`, keep Vitest script optional for experiments only and clearly marked.

    Run frozen lockfile reconciliation if the command change or script dependencies require it.
  </action>
  <acceptance_criteria>
    - `package.json` contains one primary frontend command and documents the chosen fallback strategy.
    - script change is compatible with Node 24 / pnpm execution.
  </acceptance_criteria>
  <verify>
    node -e "const p=require('./package.json'); if(!p.scripts||!p.scripts.test) {console.error('missing test script'); process.exit(1)}; if (!/^NODE_ENV=test\\b/.test(p.scripts.test)) {console.log('non-recommended env prefix:', p.scripts.test)}"
    test -f package.json
    pnpm install --frozen-lockfile
    node -e "const p=require('./package.json'); console.log(p.scripts.test)"
  </verify>
</task>

<task type="auto">
  <name>Task 3: Stabilize test environment contract</name>
  <read_first>
    jest.config.js
    spec/support/jest-setup.js
    .planning/phases/16-frontend-test-runner-modernization/16-01-PLAN.md
    package.json
  </read_first>
  <action>
    Verify and adjust test env contract for deterministic pnpm execution:
    - confirm `jest.config.js` keeps `modulePaths`, YAML transform, and test setup aligned with current code,
    - ensure `spec/support/jest-setup.js` assumptions are explicitly called out in summary,
    - avoid touching production runtime files unless command changes require environment-facing support.

    Add/maintain a short comment in `16-01-SUMMARY.md` for TEST-03 covering:
    - Node 24 + pnpm constraints,
    - what React 16/Flow/Jest APIs are preserved in phase-16 command behavior.
  </action>
  <acceptance_criteria>
    - Test environment checks are represented with explicit file-level references in the summary.
    - No production runtime file (layouts, webpack, Shakapacker, packs) changes are required for this phase.
  </acceptance_criteria>
  <verify>
    node -e "const cfg=require('./jest.config.js'); const ok=['modulePathIgnorePatterns','modulePaths','setupFilesAfterEnv','transform'].every(k=>Object.prototype.hasOwnProperty.call(cfg,k)); if(!ok){console.error('jest.config incomplete'); process.exit(1)}"
    rg -n "NODE_ENV|setupFilesAfterEnv|modulePaths|jest-dom|jsdom|yaml-jest" jest.config.js spec/support/jest-setup.js
  </verify>
</task>

<task type="auto">
  <name>Task 4: Execute representative verification and final runner summary</name>
  <read_first>
    .planning/phases/16-frontend-test-runner-modernization/16-01-SUMMARY.md
    package.json
    jest.config.js
    .planning/phases/12-vitest-and-vite-feasibility/12-01-SUMMARY.md
  </read_first>
  <action>
    Run representative verification under the chosen runner:
    - if fallback Jest: run `pnpm test -- --runInBand` and at least one representative test in `app/javascript/shared/__tests__`.
    - if Vitest lane is switched by executor: run the selected vitest command and include explicit blocker status.

    Update `16-01-SUMMARY.md` with:
    - command result matrix (pass/fail),
    - `TEST-01`..`TEST-04` mapping,
    - QA-04 note and whether route-level QA is required.

    If representative suite is blocked by environment transforms, keep route QA unchanged and classify blocker with a fix path.
  </action>
  <acceptance_criteria>
    - `16-01-SUMMARY.md` includes explicit selected runner, command matrix, and VITE-02/VITE-03 lane outcome.
    - TEST-01..TEST-04 evidence is present and complete enough to start execution acceptance.
    - `QA-04` execution note exists for touched tests.
  </acceptance_criteria>
  <verify>
    pnpm test -- --runInBand
    pnpm test -- --runInBand app/javascript/shared/__tests__/functions.test.js
    rg -n "VITE-02|VITE-03|TEST-01|TEST-02|TEST-03|TEST-04|QA-04|runner" .planning/phases/16-frontend-test-runner-modernization/16-01-SUMMARY.md
  </verify>
</task>

</tasks>

<threat_model>
| Threat | Category | Component | Mitigation |
|---|---|---|---|
| T-16-01 | Configuration drift | Testing behavior | Use one canonical command and lock all runner updates to PLAN scope. |
| T-16-02 | Runner mismatch | CI/Developer parity | Keep command pnpm-backed and verify with `pnpm test -- --runInBand`. |
| T-16-03 | Hidden transform breakage | Transform/JS tests | Verify `jest.config.js` contract explicitly and record TEST-03 setup assumptions. |
| T-16-04 | Vitest migration risk | Mocking API parity | If Vitest lane is attempted, keep migration scoped and explicit blockers in summary. |
</threat_model>

<verification>
Run after all tasks:
- `pnpm install --frozen-lockfile`
- `pnpm test -- --runInBand`
- `pnpm test -- --runInBand app/javascript/shared/__tests__/functions.test.js`
- `rg -n "VITE-02|VITE-03|TEST-01|TEST-02|TEST-03|TEST-04|QA-04|summary|fallback|runner" .planning/phases/16-frontend-test-runner-modernization/16-01-SUMMARY.md`
- `node -e "const cfg=require('./jest.config.js'); console.log([Object.keys(cfg).join(','), cfg.modulePaths] )"`
</verification>

<source_audit>
- VITE-02: Explicitly considered against Phase 12 feasibility evidence.
- VITE-03: Documented fallback lane if Vitest compatibility remains incomplete.
- TEST-01: Single command evidence in `package.json` and task verification.
- TEST-02: Representative test suites are required in Task 4.
- TEST-03: Node/Jest environment assumptions explicitly documented in summary.
- TEST-04: Config/transform checks and test output requirements are explicit.
- QA-04: Task 4 requires targeted frontend verification note.
</source_audit>

<success_criteria>
- A single pnpm-backed frontend test command is defined and verified.
- TEST-01..TEST-04 are explicitly mapped to command outcomes.
- VITE-02 vs VITE-03 outcome is explicit and consistent with Phase 12 blockers.
- `16-01-SUMMARY.md` records test command, runner lane, and next corrective step if blocked.
</success_criteria>

<output>
Create `.planning/phases/16-frontend-test-runner-modernization/16-01-SUMMARY.md` with command evidence and final runner decision.
</output>
