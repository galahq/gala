---
phase: 10-dependency-target-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md
autonomous: true
requirements:
  - DEPS-01
  - DEPS-02
  - DEPS-03
  - DEPS-04
must_haves:
  truths:
    - "D-01: Later phases can see current official registry/doc evidence and exact recheck commands before editing dependencies; Phase 10 leaves package manifests, lockfiles, package-manager config, build config, and test-runner config untouched."
    - "D-02: Phase 10 is policy and target selection only; pnpm migration, Flow removal, Vitest/Vite spikes, Ruby updates, JavaScript updates, and visual regression setup remain assigned to later phases."
    - "D-03: Dependency target selection uses current official registry/doc data at implementation time, and downstream phases must re-check official sources before editing manifests or lockfiles."
    - "D-04: React is explicitly held to the v1.1 React 16.x constraint, with React major upgrades assigned outside v1.1."
    - "D-05: BlueprintJS is explicitly held to the v1.1 BlueprintJS 4.x constraint, with BlueprintJS major upgrades assigned outside v1.1."
    - "D-06: Shakapacker/Webpack production behavior remains the default unless Phase 12 proves Vite replacement safe."
    - "DEPS-04: Every target below latest has a compatibility reason and owner phase."
  artifacts:
    - path: ".planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md"
      provides: "Dependency target baseline, compatibility holds, source evidence, and recheck policy"
      contains: "DEPS-01"
  key_links:
    - from: ".planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md"
      to: ".planning/phases/10-dependency-target-baseline/10-RESEARCH.md"
      via: "matrix rows sourced from Phase 10 research"
      pattern: "Latest checked|Recheck|npm view|rubygems.org|ruby-lang.org"
    - from: ".planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md"
      to: ".planning/REQUIREMENTS.md"
      via: "requirement markers embedded in matrix sections"
      pattern: "DEPS-01|DEPS-02|DEPS-03|DEPS-04"
    - from: ".planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md"
      to: "package.json and Gemfile"
      via: "current-state rows only; no edits to source manifests"
      pattern: "Current|v1.1 target|Holdback reason|Owner phase"
---

<objective>
Create the Phase 10 dependency target baseline matrix.

Purpose: Establish exact dependency targets, compatibility holds, and verification policy before any later phase changes lockfiles or manifests.
Output: `.planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md`.
</objective>

<execution_context>
@/Users/nathanpapes/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nathanpapes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dependency-target-baseline/10-CONTEXT.md
@.planning/phases/10-dependency-target-baseline/10-RESEARCH.md
@.planning/phases/10-dependency-target-baseline/10-PATTERNS.md
@.planning/phases/10-dependency-target-baseline/10-VALIDATION.md
@.planning/codebase/STACK.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@Gemfile
@Gemfile.lock
@package.json
@yarn.lock
@.ruby-version
@.node-version

<interfaces>
Phase 10 has no code interface. The documentation artifact must expose an executable matrix with these columns, adapting only if a clearer equivalent preserves all fields:
`Requirement`, `Dependency family`, `Package`, `Current`, `Latest checked`, `v1.1 target`, `Decision`, `Holdback reason`, `Owner phase`, `Source`, `Recheck`.

Locked decisions from `10-CONTEXT.md`:
- D-01: Use a conservative matrix, explicitly hold React 16.8 and BlueprintJS 4.x, and make no dependency, package manager, lockfile, or build-runner changes in this phase.
- D-02: Phase 10 is policy and target selection only; later phases perform pnpm migration, Flow removal, Vitest/Vite spikes, Ruby updates, JavaScript updates, and visual regression setup.
- D-03: Dependency target selection uses current official registry or documentation data at implementation time, and downstream phases must re-check official sources before editing manifests or lockfiles.
- D-04: React remains on the current 16.8 line for v1.1.
- D-05: BlueprintJS remains on the current 4.x line for v1.1.
- D-06: Shakapacker/Webpack production behavior remains the default unless the Vite feasibility phase later proves replacement is safe.
</interfaces>
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Establish exact dependency targets, compatibility holds, and verification policy before changing lockfiles | Covered by Tasks 1-3 |
| REQ | DEPS-01: current official registry/doc data at implementation time | Covered by Tasks 1 and 3 |
| REQ | DEPS-02: React remains on current 16.8 line during v1.1 | Covered by Task 2 |
| REQ | DEPS-03: BlueprintJS remains on current 4.x line during v1.1 | Covered by Task 2 |
| REQ | DEPS-04: held dependencies below latest documented with compatibility reason | Covered by Tasks 2 and 3 |
| RESEARCH | Matrix rows include current, latest checked, target, decision, reason, owner phase, source, and recheck command | Covered by Tasks 1-3 |
| RESEARCH | Capture Ruby, Rails, pnpm, TypeScript, Vitest/Vite, Playwright, Shakapacker/Webpack, and key runtime gems | Covered by Task 2 |
| CONTEXT | D-01 conservative matrix and no manifest/lock/build-runner changes | Covered by Tasks 1 and 3 |
| CONTEXT | D-02 later phases own implementation changes | Covered by Tasks 2 and 3 |
| CONTEXT | D-03 official recheck policy | Covered by Tasks 1 and 3 |
| CONTEXT | D-04 React 16 hold | Covered by Task 2 |
| CONTEXT | D-05 BlueprintJS 4 hold | Covered by Task 2 |
| CONTEXT | D-06 Shakapacker/Webpack default unless Vite proves replacement safe | Covered by Task 2 |

Deferred ideas: none.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Create the matrix shell and official-source policy</name>
  <read_first>
    @.planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md if it already exists
    @.planning/phases/10-dependency-target-baseline/10-CONTEXT.md
    @.planning/phases/10-dependency-target-baseline/10-RESEARCH.md
    @.planning/phases/10-dependency-target-baseline/10-PATTERNS.md
    @.planning/phases/10-dependency-target-baseline/10-VALIDATION.md
    @.planning/REQUIREMENTS.md
    @.planning/ROADMAP.md
    @Gemfile
    @Gemfile.lock
    @package.json
    @yarn.lock
    @.ruby-version
    @.node-version
  </read_first>
  <files>.planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md</files>
  <action>Create `10-DEPENDENCY-MATRIX.md` with frontmatter or a clear heading identifying Phase 10. Include the phase goal, a "No Change Boundary" stating D-01 and D-02 exactly in effect, and a "Registry/Documentation Recheck Policy" for DEPS-01 per D-03. Define the matrix schema with the columns `Requirement`, `Dependency family`, `Package`, `Current`, `Latest checked`, `v1.1 target`, `Decision`, `Holdback reason`, `Owner phase`, `Source`, and `Recheck`. Add the npm, RubyGems, and Ruby downloads recheck commands from `10-RESEARCH.md`; do not run install/update commands and do not edit any package manifest, lockfile, build config, or test config.</action>
  <verify>
    <automated>test -f .planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md && rg -n "DEPS-01|No Change Boundary|Registry/Documentation Recheck Policy|Latest checked|Source|Recheck|npm view|rubygems.org|ruby-lang.org" .planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - `10-DEPENDENCY-MATRIX.md` exists.
    - The file contains `DEPS-01`.
    - The file contains exact searchable headings or labels for `No Change Boundary`, `Registry/Documentation Recheck Policy`, `Latest checked`, `Source`, and `Recheck`.
    - The file contains `npm view`, `rubygems.org`, and `ruby-lang.org` recheck references.
    - No files are modified except `.planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md`.
  </acceptance_criteria>
  <done>The dependency matrix shell exists and makes official-source rechecks mandatory before later dependency edits.</done>
</task>

<task type="auto">
  <name>Task 2: Fill dependency targets, holds, and owner phases</name>
  <read_first>
    @.planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md
    @.planning/phases/10-dependency-target-baseline/10-CONTEXT.md
    @.planning/phases/10-dependency-target-baseline/10-RESEARCH.md
    @.planning/phases/10-dependency-target-baseline/10-PATTERNS.md
    @.planning/REQUIREMENTS.md
    @.planning/ROADMAP.md
    @.planning/codebase/STACK.md
    @.planning/codebase/CONCERNS.md
    @Gemfile
    @Gemfile.lock
    @package.json
    @yarn.lock
    @.ruby-version
    @.node-version
  </read_first>
  <files>.planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md</files>
  <action>Populate the matrix rows from `10-RESEARCH.md` and current manifests. Include, at minimum, Ruby, Rails, pnpm, TypeScript, Vitest, Vite, Playwright or `@playwright/test`, Shakapacker gem/npm, Webpack, React, React DOM, `@blueprintjs/core`, `@blueprintjs/datetime`, `@blueprintjs/select`, Puma, Sidekiq, pg, redis, Sentry gems, Devise, RSpec Rails, Capybara, and Selenium WebDriver. Apply the resolved React decision by recording the v1.1 target as the existing React 16 baseline (`package.json` `^16.8.6`, current lock 16.12.0), listing React 16.14.0 only as a Phase 15 candidate, and recording React 19/latest major as outside v1.1. Apply the resolved BlueprintJS decision by marking BlueprintJS as a v1.1 hold on 4.x, listing `@blueprintjs/select@4.9.24` only as a Phase 15 candidate requiring visual QA, and recording BlueprintJS 6/React 18 peer requirements as outside v1.1. Apply the resolved Ruby decision by marking Ruby 4.0.4 as a Phase 14 candidate requiring Rails boot, targeted RSpec, Dockerfile/runtime image review, and deployment parity checks before changing `.ruby-version`. Apply D-06 by marking Shakapacker/Webpack as the default production bundler unless Phase 12 proves Vite replacement safe. Assign owner phases using the roadmap: Phase 11 for pnpm, Phase 12 for Vitest/Vite feasibility, Phase 13 for TypeScript/Flow removal targets, Phase 14 for Ruby/runtime gems, Phase 15 for JavaScript dependencies and Shakapacker/Webpack, Phase 16 for frontend test runner, and Phase 17 for Playwright visual regression.</action>
  <verify>
    <automated>rg -n "DEPS-02|DEPS-03|DEPS-04|React.*16|React.*19|BlueprintJS.*4|BlueprintJS.*6|React 18|Shakapacker|Webpack|pnpm|TypeScript|Vitest|Vite|Playwright|Puma|Sidekiq|Holdback reason|Owner phase" .planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - The matrix contains rows or grouped rows for all dependency families named in the Phase 10 roadmap success criteria.
    - `DEPS-02` appears near the React hold rows, and those rows include `React`, `16.12.0`, `16.14.0`, and `19`.
    - `DEPS-03` appears near the BlueprintJS hold rows, and those rows include `BlueprintJS`, `4.9.24`, `6`, `React 18`, and `visual QA`.
    - `DEPS-04` appears in the holdback section or hold rows.
    - Every row with `Decision` equal to `hold`, `candidate`, `spike`, or an equivalent below-latest target has a non-empty `Holdback reason` and `Owner phase`.
  </acceptance_criteria>
  <done>The matrix gives later phases exact target and hold policy for all dependency families without changing manifests.</done>
</task>

<task type="auto">
  <name>Task 3: Add self-audit checks and prove Phase 10 stayed documentation-only</name>
  <read_first>
    @.planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md
    @.planning/phases/10-dependency-target-baseline/10-VALIDATION.md
    @.planning/phases/10-dependency-target-baseline/10-PATTERNS.md
    @.planning/REQUIREMENTS.md
    @.planning/ROADMAP.md
    @Gemfile
    @Gemfile.lock
    @package.json
    @yarn.lock
    @.ruby-version
    @.node-version
  </read_first>
  <files>.planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md</files>
  <action>Add a final "Verification Checklist" section to the matrix. Include exact documentation audit commands for DEPS-01 through DEPS-04 from `10-VALIDATION.md`, plus a no-change audit command covering `Gemfile`, `Gemfile.lock`, `package.json`, `yarn.lock`, `pnpm-lock.yaml`, `config/webpack`, `config/shakapacker.yml`, and `jest.config.js`. Because the worktree may already contain user changes, the checklist must state that the executor may only modify `10-DEPENDENCY-MATRIX.md`; any pre-existing manifest or lockfile diffs must be reported as pre-existing and not normalized, edited, or reverted by this plan.</action>
  <verify>
    <automated>rg -n "Verification Checklist|DEPS-01|DEPS-02|DEPS-03|DEPS-04|git diff -- Gemfile Gemfile.lock package.json yarn.lock pnpm-lock.yaml config/webpack config/shakapacker.yml jest.config.js|pre-existing|10-DEPENDENCY-MATRIX.md" .planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md && git diff --name-only -- .planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - The matrix contains a `Verification Checklist` section.
    - The checklist contains separate commands or rows for DEPS-01, DEPS-02, DEPS-03, and DEPS-04.
    - The checklist contains the exact no-change audit command `git diff -- Gemfile Gemfile.lock package.json yarn.lock pnpm-lock.yaml config/webpack config/shakapacker.yml jest.config.js`.
    - `git diff --name-only -- .planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md` lists only the matrix path for this plan's intended edit.
    - Any existing `package.json` or `yarn.lock` diffs remain untouched and are described in the summary as pre-existing if still present.
  </acceptance_criteria>
  <done>The matrix is self-auditable and the executor has proven Phase 10 only created the documentation baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Official registries/docs -> planning artifact | Version and compatibility evidence crosses from external registries/docs into a committed local policy document. |
| Planning artifact -> later implementation phases | Later agents may use the matrix to decide what dependency edits are allowed. |
| Documentation-only phase -> working tree | The executor can write files, but Phase 10 must not alter manifests, lockfiles, build config, or test config. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Spoofing | `10-DEPENDENCY-MATRIX.md` source evidence | mitigate | Require official `npm view`, RubyGems API, and ruby-lang.org source/recheck fields for DEPS-01. |
| T-10-02 | Tampering | `Gemfile`, `Gemfile.lock`, `package.json`, `yarn.lock`, build/test config | mitigate | Keep `files_modified` to `10-DEPENDENCY-MATRIX.md` and include the exact no-change audit command in Task 3. |
| T-10-03 | Repudiation | Holdback decisions | mitigate | Require requirement markers, locked decision references D-01 through D-06, owner phase, and holdback reason fields. |
| T-10-04 | Information Disclosure | Registry/source capture | accept | The matrix uses public package version and compatibility data only; no secrets or private config are required. |
| T-10-05 | Denial of Service | Later dependency phases | mitigate | Separate `Latest checked` from `v1.1 target` and require later rechecks so stale data does not force broken upgrades. |
| T-10-06 | Elevation of Privilege | Build/test tool replacement decisions | mitigate | Record Vite production replacement as Phase 12 spike-gated and Shakapacker/Webpack as default per D-06. |
</threat_model>

<verification>
Run these after all tasks:

<automated>test -f .planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md</automated>
<automated>rg -n "DEPS-01|DEPS-02|DEPS-03|DEPS-04|React.*16|BlueprintJS.*4|Latest checked|Source|Recheck|Holdback reason|Owner phase|Verification Checklist" .planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md</automated>
<automated>git diff -- Gemfile Gemfile.lock package.json yarn.lock pnpm-lock.yaml config/webpack config/shakapacker.yml jest.config.js</automated>

The final `git diff` command is an audit, not permission to edit or revert those files. If it shows pre-existing user changes, report them as pre-existing and leave them untouched.
</verification>

<success_criteria>
- `.planning/phases/10-dependency-target-baseline/10-DEPENDENCY-MATRIX.md` exists.
- The matrix covers DEPS-01, DEPS-02, DEPS-03, and DEPS-04.
- React 16.x and BlueprintJS 4.x holds are explicit v1.1 constraints.
- Every below-latest target or hold has a compatibility reason and owner phase.
- Later phases have exact recheck commands for official registry/doc data.
- No package manifest, lockfile, package-manager, build, or test-runner config file is modified by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dependency-target-baseline/10-dependency-target-baseline-01-SUMMARY.md`.
</output>
