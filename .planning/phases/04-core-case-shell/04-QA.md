# Phase 04 Core Case Shell QA

## Data Selection

- chosen slug (published): `3880fdc8-0a48-4e5f-b146-9deffd7f9c22`
- Source: `docker compose exec web bin/rails runner 'Case.limit(20).pluck(:id, :slug, :published_at)'`
- Published status: published at `2026-01-18T14:33:33Z`
- Editor-accessible slug: not available from existing local data. `Editorship.joins(:case, :editor).limit(20)` returned no rows.
- Existing reader account discovered: `dev@learnmsc.org`; no existing local credential documentation or editorship coverage was found.
- Host Ruby note: host-side `bin/rails runner` cannot run because the shell Ruby is `2.6.10` while the app requires Ruby `4.0.3`; Docker runner is used for app/database inspection.

## Route Checklist

| Route family | Representative URL/request | Auth target | Data | Browser/spec status |
| --- | --- | --- | --- | --- |
| GET `/cases.json` or equivalent case index JSON request | `GET http://localhost:3000/cases.json` | anonymous | published local cases | pending browser/spec status |
| GET `/cases/:slug` | `GET http://localhost:3000/cases/3880fdc8-0a48-4e5f-b146-9deffd7f9c22` | anonymous | chosen slug | pending browser/spec status |
| GET `/cases/:slug/edit` | `GET http://localhost:3000/cases/3880fdc8-0a48-4e5f-b146-9deffd7f9c22/edit` | editor | no existing editorship | substitution: controller/feature specs supplement editor redirect/edit-mode coverage |
| PATCH/PUT `/cases/:slug` | `PATCH /cases/3880fdc8-0a48-4e5f-b146-9deffd7f9c22` | editor | no existing editorship | substitution: `spec/controllers/cases_controller_spec.rb` supplements update/auth route behavior where practical |
| DELETE `/cases/:slug` redirect/confirmation behavior | `DELETE /cases/3880fdc8-0a48-4e5f-b146-9deffd7f9c22` and `GET /cases/:slug/confirm_deletion` | editor | no existing editorship | substitution: `spec/features/deleting_a_case_spec.rb` covers confirmation behavior with factory editorship |
| GET `/cases/:slug/copy` | `GET http://localhost:3000/cases/3880fdc8-0a48-4e5f-b146-9deffd7f9c22/copy` | editor | no existing editorship | substitution: authenticated browser not available; route redirect behavior will be recorded if reachable |
| GET `/cases/:slug/archive` | `GET http://localhost:3000/cases/3880fdc8-0a48-4e5f-b146-9deffd7f9c22/archive` | editor | no existing editorship | substitution: authenticated browser not available; unauthenticated redirect/result will be recorded |
| GET/PATCH `/cases/:slug/settings` | `GET http://localhost:3000/cases/3880fdc8-0a48-4e5f-b146-9deffd7f9c22/settings/edit` and `PATCH /cases/:slug/settings` | editor | no existing editorship | substitution: authenticated browser not available; settings view inspected statically and specs supplement |
| GET/POST `/cases/:slug/translations` | `GET http://localhost:3000/cases/3880fdc8-0a48-4e5f-b146-9deffd7f9c22/translations/new` and `POST /cases/:slug/translations` | editor | no existing editorship | substitution: authenticated browser not available; route redirect behavior will be recorded if reachable |
| GET `/cases/:slug/1` or closest content-position suffix | `GET http://localhost:3000/cases/3880fdc8-0a48-4e5f-b146-9deffd7f9c22/1` | anonymous | chosen slug | pending browser/spec status |
| GET `/cases/:slug/conversation` | `GET http://localhost:3000/cases/3880fdc8-0a48-4e5f-b146-9deffd7f9c22/conversation` | anonymous | chosen slug | pending browser/spec status |

## Browser QA Results

Browser tooling reaches the Dockerized app through `host.docker.internal:3000`.
The project-facing route target remains `localhost:3000`; `curl` checks below
used `localhost:3000`.

### Authenticated Visual QA Correction

Protected-route visual QA should use the local Google OmniAuth mock before accepting
anonymous redirects as the only browser result. In the Docker Compose/browser-MCP
network, open `/readers/sign_in`, click "Sign in with Google", and rely on
`config/initializers/mock_omniauth.rb` to sign in the development mock user
`dev@learnmsc.org` / "Developer Admin". After that session exists, revisit
protected case routes such as edit, copy, archive, settings, translations, and
delete confirmation to inspect BlueprintJS parity on the actual protected
surfaces.

The anonymous protected-route checks below remain useful for authorization
boundaries, but they should not be treated as sufficient visual coverage for
protected BlueprintJS surfaces in future passes.

| Route family | URL/request checked | Auth state used | Visible/result status | console errors | network errors | visual notes |
| --- | --- | --- | --- | --- | --- | --- |
| GET `/cases.json` or equivalent case index JSON request | `curl -i -H 'Accept: application/json' http://localhost:3000/cases.json` | anonymous | pass: `200 OK`, JSON array returned with chosen slug first | none from curl | none from curl | JSON route is reachable and serialized published cases. |
| GET `/cases/:slug` | `http://host.docker.internal:3000/cases/3880fdc8-0a48-4e5f-b146-9deffd7f9c22` | anonymous | pass: nonblank case overview shell, title `Pickled Cold Pressed -- Gala` | non-blocking: known React 16/dependency warnings for `isDragging`, unsafe lifecycle methods, and render transition; non-blocking HMR `Invalid Host/Origin header` from browser-container host mismatch | non-blocking: external Mapbox style 404, previously observed route-noise unless map behavior is in scope | Overview, toolbar, cover/content blocks, sidebar TOC, and footer render. Conversation button is disabled while anonymous. |
| GET `/cases/:slug/edit` | `http://host.docker.internal:3000/cases/3880fdc8-0a48-4e5f-b146-9deffd7f9c22/edit` | anonymous; editor auth not available | pass with documented exception: redirected to `/readers/sign_in` with expected Devise message | non-blocking HMR `Invalid Host/Origin header` only | none observed beyond normal external font requests | Sign-in form renders; editor behavior requires supplemental specs. |
| PATCH/PUT `/cases/:slug` | not browser-visible without editor session | editor auth not available | not reached in browser | not applicable | not applicable | substitution: targeted controller specs supplement authenticated update behavior where practical. |
| DELETE `/cases/:slug` redirect/confirmation behavior | `GET /cases/:slug/confirm_deletion` route surface not reachable without editor session | editor auth not available | not reached in browser | not applicable | not applicable | substitution: delete confirmation is covered by `spec/features/deleting_a_case_spec.rb`; static view inspection found legacy-only delete confirmation classes but no browser-confirmed regression. |
| GET `/cases/:slug/copy` | `curl -I http://localhost:3000/cases/3880fdc8-0a48-4e5f-b146-9deffd7f9c22/copy` | anonymous | pass: `302 Found` to `/readers/sign_in` | none from curl | none from curl | Protected copy route preserves auth boundary. |
| GET `/cases/:slug/archive` | `curl -I http://localhost:3000/cases/3880fdc8-0a48-4e5f-b146-9deffd7f9c22/archive` | anonymous | pass: `302 Found` to `/readers/sign_in` | none from curl | none from curl | Protected archive route preserves auth boundary. |
| GET/PATCH `/cases/:slug/settings` | `curl -I http://localhost:3000/cases/3880fdc8-0a48-4e5f-b146-9deffd7f9c22/settings/edit` | anonymous | pass: `302 Found` to `/readers/sign_in` | none from curl | none from curl | Settings view was inspected statically; existing card/sidebar classes already carry `.bp4-*` companions. |
| GET/POST `/cases/:slug/translations` | `curl -I http://localhost:3000/cases/3880fdc8-0a48-4e5f-b146-9deffd7f9c22/translations/new` | anonymous | pass: `302 Found` to `/readers/sign_in` | none from curl | none from curl | Protected translations route preserves auth boundary. |
| GET `/cases/:slug/1` or closest content-position suffix | `http://host.docker.internal:3000/cases/3880fdc8-0a48-4e5f-b146-9deffd7f9c22/1` | anonymous | pass: nonblank case-element shell, title includes content element | non-blocking: known React 16/dependency warnings for `isDragging`, unsafe lifecycle methods, `DraftEditor`, and HMR host mismatch | none blocking; only ahoy/mini-profiler/font requests reported | Case element content renders with sidebar and next link. |
| GET `/cases/:slug/conversation` | `http://host.docker.internal:3000/cases/3880fdc8-0a48-4e5f-b146-9deffd7f9c22/conversation` | anonymous | pass with documented exception: Rails accepts the suffix and React shell loads, then client state lands at `/cases/:slug/` because anonymous conversation access is disabled | non-blocking: known React 16/dependency warnings, HMR host mismatch, and Mapbox 404 | non-blocking: external Mapbox style 404 | No Rails 404 or blank React state; full conversation content requires authenticated/enrolled reader state. |

Real app console/runtime errors marked blocking: none for Phase 4 shell routing.
Known non-blocking noise: HMR `Invalid Host/Origin header` from
`host.docker.internal`, external Mapbox style `404`, and existing React
warnings from legacy dependencies/components.

Editor auth not available: using existing local data only, there is one reader
(`dev@learnmsc.org`), no editorship rows, and the likely fixture password
`secret` failed with `Invalid Email or password.` No editor-authenticated browser
session was available without mutating local data.

## Fixes Applied

None. Browser QA and static inspection did not prove a Phase 4 visual/runtime
regression that justified changing code under the narrow-fix rule.

No code change was made for the legacy-only classes in
`app/views/cases/deletions/new.html.haml` because the delete confirmation route
was not reachable with existing editor credentials and existing feature specs
already cover its behavior. This remains a documented inspection target for a
future authenticated pass rather than a Phase 4 browser-confirmed fix.

## Automated Gates

| Command | Result | Notes |
| --- | --- | --- |
| `docker compose exec web bundle exec rspec spec/controllers/cases_controller_spec.rb spec/features/viewing_a_case_spec.rb spec/features/deleting_a_case_spec.rb spec/features/publishing_a_case_spec.rb` | failed: environment-blocked feature specs | Controller examples ran, but all 11 feature-spec failures shared Selenium setup error `ArgumentError: Can't initialize Selenium::WebDriver::Chrome::Driver with :url`. This blocked browser-feature supplement coverage before route assertions. |
| `docker compose exec web bundle exec rspec spec/controllers/cases_controller_spec.rb` | pass | `5 examples, 0 failures`; confirms case create, show, and React suffix controller routing. |
| `yarn test app/javascript/utility/__tests__/Toolbar.test.jsx` | skipped | `Toolbar.jsx` and toolbar expectations were not changed; browser QA did not prove a toolbar regression. |

## Phase 4 Gate

pass with documented exceptions

Exceptions:

- Editor-authenticated browser coverage was not available from existing local
  data/credentials. Protected case routes were verified as preserving the
  anonymous sign-in redirect, and controller specs cover the core case route
  behavior.
- Feature-spec supplement coverage for viewing/deleting/publishing was attempted
  but blocked by the local Selenium/Capybara driver setup error
  `Can't initialize Selenium::WebDriver::Chrome::Driver with :url`.
