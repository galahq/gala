---
phase: 01-infra-safety-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - config/routes.rb
  - infra/sst.config.ts
  - spec/requests/health_check_spec.rb
autonomous: true
requirements:
  - HLTH-01
  - HLTH-02
  - HLTH-03
must_haves:
  truths:
    - "A dedicated unauthenticated health endpoint responds successfully without rendering the catalog root."
    - "SST ALB target health uses the dedicated health endpoint instead of `/`."
    - "SST ECS container health uses the dedicated health endpoint instead of `/`."
  artifacts:
    - path: config/routes.rb
      provides: "Dedicated Rails health route"
      contains: "up"
    - path: infra/sst.config.ts
      provides: "SST health check path and container curl command"
      contains: "/up"
    - path: spec/requests/health_check_spec.rb
      provides: "Request coverage for the health endpoint"
  key_links:
    - from: infra/sst.config.ts
      to: config/routes.rb
      via: "health check path"
      pattern: "/up"
---

<objective>
Add the shallow health endpoint and wire SST health checks to it.

Purpose: Health checks must no longer depend on the catalog root page or user-facing rendering before AWS staging validation.
Output: Rails route/spec plus SST ALB and ECS health check updates.
</objective>

<execution_context>
@/Users/nathanpapes/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nathanpapes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-infra-safety-baseline/01-CONTEXT.md

Relevant files:
@config/routes.rb
@infra/sst.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a shallow health route</name>
  <files>config/routes.rb, spec/requests/health_check_spec.rb</files>
  <action>Add `GET /up` as an unauthenticated, shallow Rack/Rails response that returns HTTP 200 and a small plain-text body. Add a request spec proving `GET /up` succeeds and does not redirect to authentication.</action>
  <verify>bundle exec rspec spec/requests/health_check_spec.rb</verify>
  <done>`GET /up` has request coverage and does not depend on the catalog root route.</done>
</task>

<task type="auto">
  <name>Task 2: Point SST web health checks at /up</name>
  <files>infra/sst.config.ts</files>
  <action>Update the `GalaWeb` ALB target health path and container health `curl` command from `/` to `/up`. Remove comments that describe `/` as temporary health behavior.</action>
  <verify>rg -n 'path: "/up"|localhost:3000/up|path: "/"|localhost:3000/ ' infra/sst.config.ts</verify>
  <done>SST health checks reference `/up`; web health no longer checks `/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet -> health endpoint | Unauthenticated public HTTP request reaches a shallow app endpoint. |
| SST config -> Rails route | Infrastructure health checks assume the app exposes the configured path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information disclosure | `/up` response | mitigate | Return only a generic status body; do not expose env, versions, database state, or secrets. |
| T-01-02 | Denial of service | `/up` endpoint | mitigate | Keep endpoint constant-time and avoid database, Redis, external network calls, or page rendering. |
</threat_model>

<verification>
- `bundle exec rspec spec/requests/health_check_spec.rb`
- `rg -n 'path: "/up"|localhost:3000/up' infra/sst.config.ts`
- `gsd-sdk query frontmatter.validate .planning/phases/01-infra-safety-baseline/01-01-PLAN.md --schema plan`
</verification>

<success_criteria>
- `GET /up` is available without authentication.
- SST ALB health path is `/up`.
- SST container health command curls `/up`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infra-safety-baseline/01-01-SUMMARY.md`.
</output>
