---
phase: 01-infra-safety-baseline
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - config/storage.yml
  - docs/aws-sst-phase-1-preflight.md
autonomous: true
requirements:
  - DPLY-04
  - STOR-01
  - STOR-02
must_haves:
  truths:
    - "Active Storage production S3 config can use AWS SDK provider-chain credentials in ECS."
    - "Static AWS access key env vars are not required by `config/storage.yml` for the `amazon` service."
    - "Phase 1 preflight documents how to validate the production image build path before staging."
  artifacts:
    - path: config/storage.yml
      provides: "IAM-friendly Active Storage S3 configuration"
    - path: docs/aws-sst-phase-1-preflight.md
      provides: "Image build and storage preflight checks"
  key_links:
    - from: config/storage.yml
      to: infra/sst.config.ts
      via: "S3 bucket/env and ECS task-role media policy"
      pattern: "S3_BUCKET|msc-gala"
---

<objective>
Patch Active Storage S3 credentials for ECS task roles and finish image/storage preflight guidance.

Purpose: Phase 2 staging should not discover that Rails requires static AWS keys or that the production image path was not validated.
Output: IAM-friendly storage config and expanded preflight checks.
</objective>

<execution_context>
@/Users/nathanpapes/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nathanpapes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-infra-safety-baseline/01-CONTEXT.md
@.planning/phases/01-infra-safety-baseline/01-02-SUMMARY.md

Relevant files:
@config/storage.yml
@infra/sst.config.ts
@Dockerfile
@docs/aws-sst-phase-1-preflight.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make S3 storage config IAM-friendly</name>
  <files>config/storage.yml</files>
  <action>Update the `amazon` Active Storage service so it does not require `AWS_ACCESS_KEY_ID` or `AWS_SECRET_ACCESS_KEY`. Use region and bucket environment values with safe defaults, allowing the AWS SDK provider chain to use ECS task-role credentials while still supporting local env credentials when present.</action>
  <verify>ruby -rerb -ryaml -e 'YAML.safe_load(ERB.new(File.read("config/storage.yml")).result, aliases: true); puts "storage yaml ok"' && ! rg -n 'AWS_ACCESS_KEY_ID|AWS_SECRET_ACCESS_KEY' config/storage.yml</verify>
  <done>`config/storage.yml` parses and the `amazon` service no longer names static AWS key env vars.</done>
</task>

<task type="auto">
  <name>Task 2: Complete storage and image preflight guidance</name>
  <files>docs/aws-sst-phase-1-preflight.md</files>
  <action>Update the Phase 1 preflight doc with concrete checks for storage YAML parsing/provider-chain behavior and production image/asset precompile validation. Keep full upload validation deferred to Phase 2.</action>
  <verify>rg -n 'storage.yml|provider chain|task role|docker build|assets:precompile|Phase 2' docs/aws-sst-phase-1-preflight.md</verify>
  <done>The preflight describes storage and image validation boundaries for Phase 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ECS task role -> S3 bucket | AWS identity grants app runtime access to media objects. |
| Production image build -> runtime | Build-time asset compilation creates artifacts served in production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information disclosure | S3 credentials | mitigate | Remove required static AWS key env vars from storage config and rely on IAM task roles/provider chain in ECS. |
| T-01-06 | Tampering | S3 media access policy | mitigate | Keep SST bucket policy scoped to `msc-gala` list/object actions and document staging media boundary for Phase 2 validation. |
| T-01-07 | Denial of service | production image build | mitigate | Add focused preflight guidance for production image and asset precompile checks before staging runtime validation. |
</threat_model>

<verification>
- `ruby -rerb -ryaml -e 'YAML.safe_load(ERB.new(File.read("config/storage.yml")).result, aliases: true); puts "storage yaml ok"'`
- `! rg -n 'AWS_ACCESS_KEY_ID|AWS_SECRET_ACCESS_KEY' config/storage.yml`
- `rg -n 'storage.yml|provider chain|task role|docker build|assets:precompile|Phase 2' docs/aws-sst-phase-1-preflight.md`
- `gsd-sdk query frontmatter.validate .planning/phases/01-infra-safety-baseline/01-03-PLAN.md --schema plan`
</verification>

<success_criteria>
- Active Storage S3 config no longer requires static AWS key env vars.
- Storage config still parses.
- Preflight docs include production image and storage provider-chain checks.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infra-safety-baseline/01-03-SUMMARY.md`.
</output>
