---
phase: 01-infra-safety-baseline
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - docs/aws-sst-secret-inventory.md
  - docs/aws-sst-phase-1-preflight.md
autonomous: true
requirements:
  - DPLY-01
  - DPLY-02
  - DPLY-03
  - SECR-01
  - SECR-02
  - SECR-03
  - SECR-04
must_haves:
  truths:
    - "Required runtime config names are inventoried without secret values."
    - "Deploy guardrails and root-vs-infra tooling boundaries are documented for Phase 1."
    - "Web, worker, and task environment parity is explicit for downstream planning."
  artifacts:
    - path: docs/aws-sst-secret-inventory.md
      provides: "Secret and runtime environment inventory"
    - path: docs/aws-sst-phase-1-preflight.md
      provides: "Deploy and tooling preflight checklist"
  key_links:
    - from: docs/aws-sst-secret-inventory.md
      to: infra/sst.config.ts
      via: "SST secret and sharedEnvironment names"
      pattern: "RAILS_MASTER_KEY|SECRET_KEY_BASE|sharedEnvironment"
---

<objective>
Document the Phase 1 secret/environment and deploy guardrails.

Purpose: Phase 2 staging validation needs a clear, value-free inventory of required config and a repeatable preflight that preserves the repo's root/infra tooling boundary.
Output: Secret inventory and preflight documentation.
</objective>

<execution_context>
@/Users/nathanpapes/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nathanpapes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-infra-safety-baseline/01-CONTEXT.md

Relevant files:
@infra/sst.config.ts
@.github/workflows/deploy.yml
@docs/aws-sst-migration-plan.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a value-free SST secret inventory</name>
  <files>docs/aws-sst-secret-inventory.md</files>
  <action>Create a markdown inventory that lists required and candidate runtime config names, classifies them as boot-critical, feature-critical, optional diagnostics, or generated-by-SST, and maps each to web/worker/task usage. Do not include secret values.</action>
  <verify>rg -n 'RAILS_MASTER_KEY|SECRET_KEY_BASE|LTI_KEY|SES_SMTP_USERNAME|GOOGLE_CLIENT_ID|SENTRY_DSN|DATABASE_URL|REDIS_URL' docs/aws-sst-secret-inventory.md</verify>
  <done>The inventory captures required config names, classifications, stage readiness notes, and no values.</done>
</task>

<task type="auto">
  <name>Task 2: Create a Phase 1 deploy and tooling preflight</name>
  <files>docs/aws-sst-phase-1-preflight.md</files>
  <action>Create a concise preflight document covering GitHub stage/action guardrails, production removal guardrails, `infra/` Node 20/npm commands, root app Ruby/Node/Yarn boundaries, and focused verification commands to run before Phase 2 staging validation.</action>
  <verify>rg -n 'npm ci|npx sst install|production removal|Node 12.5.0|Node >=20|assets:precompile' docs/aws-sst-phase-1-preflight.md</verify>
  <done>The preflight gives downstream agents and humans clear guardrails without changing deploy semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs -> secrets | Documentation references sensitive config names but must not expose values. |
| GitHub Actions -> AWS | Workflow assumes an AWS role and can deploy/remove stages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information disclosure | secret inventory docs | mitigate | Record names, classifications, and sources only; never record values, tokens, or derived credentials. |
| T-01-04 | Elevation of privilege | deploy workflow guardrails | mitigate | Preserve explicit stage/action inputs, OIDC role assumption, and production removal refusal; document checks rather than weakening controls. |
</threat_model>

<verification>
- `rg -n 'RAILS_MASTER_KEY|SECRET_KEY_BASE|LTI_KEY|SES_SMTP_USERNAME|DATABASE_URL|REDIS_URL' docs/aws-sst-secret-inventory.md`
- `rg -n 'npm ci|npx sst install|production removal|Node 12.5.0|Node >=20|assets:precompile' docs/aws-sst-phase-1-preflight.md`
- `gsd-sdk query frontmatter.validate .planning/phases/01-infra-safety-baseline/01-02-PLAN.md --schema plan`
</verification>

<success_criteria>
- Secret and env inventory exists and contains no values.
- Deploy and tooling preflight exists.
- The docs preserve current workflow guardrails and tooling boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infra-safety-baseline/01-02-SUMMARY.md`.
</output>
