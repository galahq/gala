---
phase: 28
plan: 28-04
title: Finalize ARM64 and Thruster architecture decisions
type: execute
wave: 4
depends_on:
  - 28-03
files_modified:
  - infra/sst.config.ts
  - scripts/deploy-sst.sh
  - .github/workflows/deploy.yml
  - .github/workflows/preview.yml
  - .github/workflows/promote-production.yml
  - .github/workflows/rollback.yml
  - docs/aws-sst-phase-1-preflight.md
  - docs/ops/workflows/rollback.md
  - docs/ops/workflows/operator-guardrails.md
  - .planning/phases/28-verify-arm-ecs-fargate-runtime-architecture-and-resolve-thru/28-DECISION.md
autonomous: false
requirements: []
requirements_addressed: []
must_haves:
  truths:
    - "D-04: Production defaults may flip to ARM64 only after dev ARM64 proof passes."
    - "D-06: Final ARM decision weighs cost, performance, future-proofing, speed, compatibility, operational simplicity, maintenance, and rollback."
    - "D-09: Incomplete or mixed ARM64 evidence keeps production on amd64 with blockers and reopening criteria."
    - "D-10: Thruster default stance is reject unless clearly useful for Gala's CloudFront/S3/Puma/ECS architecture."
    - "D-11: Thruster must improve dynamic Rails/Puma behavior rather than duplicate S3/CloudFront static serving."
    - "D-12: Thruster work remains research/reconciliation by default, not a live experiment."
    - "D-13: Current official Thruster and Rails deployment docs are checked before no-adopt is locked."
    - "D-14: Any non-static Thruster benefit requires Gala-specific cheap proof before adoption."
    - "D-15: Rejected Thruster references are removed or corrected only where active docs imply planned adoption."
    - "D-16: Final ARM64 and Thruster decisions are recorded in phase artifacts and relevant AWS/operator docs."
    - "D-17: ARM64 adoption updates only source-of-truth architecture/build paths and active production preflight docs."
    - "D-18: Non-adoption keeps amd64 default with rationale and reopening criteria."
    - "D-19: Thruster no-adopt cleanup avoids rewriting historical planning artifacts."
    - "D-20: ARM64 production default requires task-definition rollback proof."
    - "D-23: Incomplete rollback proof blocks the ARM64 production default flip."
  artifacts:
    - path: .planning/phases/28-verify-arm-ecs-fargate-runtime-architecture-and-resolve-thru/28-DECISION.md
      provides: "Final ARM64 tradeoff analysis, ARM decision, and Thruster decision."
    - path: docs/aws-sst-phase-1-preflight.md
      provides: "Active production build/preflight guidance matching the final architecture decision."
  key_links:
    - source: "Dev ARM64 and rollback evidence"
      target: "Production architecture default"
      validates: "Production defaults change only when evidence is complete."
    - source: "CloudFront/S3 static asset model"
      target: "Thruster no-adopt decision"
      validates: "Thruster is not added when it only duplicates existing edge/static behavior."
---

# Plan 28-04: Finalize ARM64 and Thruster architecture decisions

<objective>
Convert the Phase 28 evidence into final source/docs decisions: adopt ARM64 only
if the required dev and rollback proof is complete, otherwise keep amd64 with
reopening criteria, and close Thruster unless it proves a Gala-specific dynamic
path benefit.
</objective>

<tasks>

<task id="28-04-01" type="auto">
  <name>Write ARM64 tradeoff analysis and final decision</name>
  <files>.planning/phases/28-verify-arm-ecs-fargate-runtime-architecture-and-resolve-thru/28-DECISION.md</files>
  <read_first>
  - `.planning/phases/28-verify-arm-ecs-fargate-runtime-architecture-and-resolve-thru/28-DECISION.md`
  - `.planning/phases/28-verify-arm-ecs-fargate-runtime-architecture-and-resolve-thru/28-RESEARCH.md`
  - `.planning/phases/28-verify-arm-ecs-fargate-runtime-architecture-and-resolve-thru/28-CONTEXT.md`
  </read_first>
  <action>
  Add `## ARM64 Tradeoff Analysis` to `28-DECISION.md` with a table covering cost, performance, future-proofing, speed, compatibility, operational simplicity, long-term maintenance, and rollback. Use actual Phase 28 evidence first, then clearly label vendor/source claims as supporting context. Add `## Final ARM64 Decision` with exactly `Decision: adopt-arm64` if ARM64 image build, dev ECS runtime proof, and web/worker rollback proof are complete; otherwise use exactly `Decision: keep-amd64` with exact blockers and reopening criteria.
  </action>
  <verify>
  `rg -n "ARM64 Tradeoff Analysis|cost|performance|future-proofing|speed|compatibility|operational simplicity|long-term maintenance|rollback|Decision: adopt-arm64|Decision: keep-amd64|reopening criteria" .planning/phases/28-verify-arm-ecs-fargate-runtime-architecture-and-resolve-thru/28-DECISION.md`
  </verify>
  <acceptance_criteria>
  - `28-DECISION.md` contains `## ARM64 Tradeoff Analysis`.
  - The tradeoff matrix covers all requested decision dimensions.
  - `## Final ARM64 Decision` is exactly `Decision: adopt-arm64` or `Decision: keep-amd64`.
  - Missing dev runtime or rollback proof forces `Decision: keep-amd64`.
  </acceptance_criteria>
  <done>ARM64 decision is evidence-gated and includes the requested tradeoff dimensions.</done>
</task>

<task id="28-04-02" type="auto">
  <name>Align source defaults and operator docs with ARM64 decision</name>
  <files>infra/sst.config.ts, scripts/deploy-sst.sh, .github/workflows/deploy.yml, .github/workflows/preview.yml, .github/workflows/promote-production.yml, .github/workflows/rollback.yml, docs/aws-sst-phase-1-preflight.md, docs/ops/workflows/rollback.md, docs/ops/workflows/operator-guardrails.md</files>
  <read_first>
  - `.planning/phases/28-verify-arm-ecs-fargate-runtime-architecture-and-resolve-thru/28-DECISION.md`
  - `infra/sst.config.ts`
  - `scripts/deploy-sst.sh`
  - `.github/workflows/deploy.yml`
  - `.github/workflows/preview.yml`
  - `.github/workflows/promote-production.yml`
  - `.github/workflows/rollback.yml`
  - `docs/aws-sst-phase-1-preflight.md`
  - `docs/ops/workflows/rollback.md`
  - `docs/ops/workflows/operator-guardrails.md`
  </read_first>
  <action>
  If the final decision is `Decision: adopt-arm64`, change the production-capable default architecture from `x86_64` to `arm64`, update build/preflight docs from `linux/amd64` to the validated ARM64 path, and document that the first production transition must use the full SST task-definition deployment before ECS-only image promotion resumes. If the final decision is `Decision: keep-amd64`, keep `x86_64` and `linux/amd64` defaults and update docs with exact blockers plus reopening criteria. Do not change workflow confirmations, CODEOWNER boundaries, or production deploy gates.
  </action>
  <verify>
  `cd infra && npm exec tsc -- --noEmit`
  `bash -n scripts/deploy-sst.sh`
  `ruby -e 'require "yaml"; %w[.github/workflows/deploy.yml .github/workflows/preview.yml .github/workflows/promote-production.yml .github/workflows/rollback.yml].each { |f| YAML.load_file(f); puts f }'`
  `rg -n "Decision: adopt-arm64|Decision: keep-amd64|GALA_CONTAINER_ARCHITECTURE|linux/arm64|linux/amd64|x86_64|arm64|reopening criteria|full SST|rollback" infra/sst.config.ts scripts/deploy-sst.sh .github/workflows docs/aws-sst-phase-1-preflight.md docs/ops/workflows/rollback.md docs/ops/workflows/operator-guardrails.md .planning/phases/28-verify-arm-ecs-fargate-runtime-architecture-and-resolve-thru/28-DECISION.md`
  </verify>
  <acceptance_criteria>
  - Source defaults match the final ARM decision.
  - Adopted ARM64 docs name the full-SST first-transition guardrail and rollback proof.
  - Kept amd64 docs name blockers and reopening criteria.
  - Workflow and CODEOWNER safety gates are not weakened.
  </acceptance_criteria>
  <done>Active source defaults and operator docs match the final ARM64 decision.</done>
</task>

<task id="28-04-03" type="auto">
  <name>Close Thruster decision and clean active references</name>
  <files>.planning/phases/28-verify-arm-ecs-fargate-runtime-architecture-and-resolve-thru/28-DECISION.md, docs/aws-sst-phase-1-preflight.md, docs/ops/workflows/operator-guardrails.md</files>
  <read_first>
  - `.planning/phases/28-verify-arm-ecs-fargate-runtime-architecture-and-resolve-thru/28-RESEARCH.md`
  - `.planning/phases/28-verify-arm-ecs-fargate-runtime-architecture-and-resolve-thru/28-CONTEXT.md`
  - `.planning/phases/24-cut-gala-production-docker-image-size-with-reusable-base-ima/24-CONTEXT.md`
  - `docs/aws-sst-phase-1-preflight.md`
  - `docs/ops/workflows/operator-guardrails.md`
  - `Dockerfile.production`
  - `Gemfile`
  </read_first>
  <action>
  Add `## Thruster Decision` to `28-DECISION.md`. Use exactly `Decision: no-adopt` unless the current official Rails and Basecamp Thruster docs show a non-static dynamic-path or operational benefit that maps to Gala and has cheap proof. Search active docs and runtime files for `Thruster`, `thruster`, and `thrust`; remove or update only active references that imply Thruster is planned, required, or recommended. Do not edit historical Phase 24 planning artifacts. The no-adopt rationale must name CloudFront/S3 static asset serving and ECS Puma dynamic compute.
  </action>
  <verify>
  `rg -n "Thruster|thruster|thrust" docs Dockerfile.production Gemfile .planning/ROADMAP.md .planning/phases/28-verify-arm-ecs-fargate-runtime-architecture-and-resolve-thru/28-DECISION.md`
  </verify>
  <acceptance_criteria>
  - `28-DECISION.md` contains `## Thruster Decision`.
  - The decision is exactly `Decision: no-adopt` unless a Gala-specific dynamic-path proof is recorded.
  - Active docs/runtime files do not imply Thruster is required, planned, or recommended.
  - Historical Phase 24 planning artifacts are not rewritten.
  </acceptance_criteria>
  <done>Thruster is closed as no-adopt or backed by a specific dynamic-path proof; static-serving duplication is not accepted.</done>
</task>

</tasks>

<verification>
- `cd infra && npm exec tsc -- --noEmit`
- `bash -n scripts/deploy-sst.sh`
- Workflow YAML parse for deploy, preview, promote-production, and rollback.
- `rg` checks for final ARM64 decision, reopening criteria, rollback guardrails, and Thruster references.
</verification>

<success_criteria>
- `28-DECISION.md` contains final ARM64 and Thruster decisions.
- ARM64 is adopted only with complete image, dev runtime, and rollback proof.
- If proof is incomplete, amd64 remains default with blockers and reopening criteria.
- Thruster is rejected unless a current Gala-specific dynamic-path benefit is proven.
- Active docs match the final decisions without broad historical rewrites.
</success_criteria>
