---
phase: 01-baseline-asset-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/layouts/application.html.erb
  - app/assets/stylesheets/application.css
  - app/javascript/packs/styles.js
  - config/webpack/environment.js
  - config/shakapacker.yml
  - app/javascript/shared/__tests__/blueprintAssetContract.test.js
autonomous: true
requirements:
  - FOUND-02
  - QA-03
must_haves:
  truths:
    - "Application layout loads the global Shakapacker helper pack and Sprockets application stylesheet in the intentional order."
    - "Blueprint package CSS is loaded once through Sprockets application.css, not through the Shakapacker styles pack."
    - "Targeted automated checks fail if Blueprint package CSS is reintroduced into app/javascript/packs/styles.js."
  artifacts:
    - path: "app/views/layouts/application.html.erb"
      provides: "Global Rails layout asset order"
      contains: "collected_javascript_pack_tag 'styles', 'controllers', 'onboarding'"
    - path: "app/assets/stylesheets/application.css"
      provides: "Sprockets-owned Blueprint package CSS manifest"
      contains: "require @blueprintjs/core/lib/css/blueprint"
    - path: "app/javascript/packs/styles.js"
      provides: "Gala overrides and Blueprint JavaScript helper entry"
      contains: "import 'shared/blueprintLegacyNamespace'"
    - path: "app/javascript/shared/__tests__/blueprintAssetContract.test.js"
      provides: "Regression tests for global Blueprint asset ownership"
  key_links:
    - from: "app/views/layouts/application.html.erb"
      to: "app/assets/stylesheets/application.css"
      via: "stylesheet_link_tag 'application'"
      pattern: "stylesheet_link_tag 'application'"
    - from: "app/javascript/packs/styles.js"
      to: "app/javascript/shared/blueprintLegacyNamespace.js"
      via: "import"
      pattern: "shared/blueprintLegacyNamespace"
---

<objective>
Establish the global asset-loading contract for Phase 1.

Purpose: Phase 1 needs a stable baseline before route-specific work. Per D-05, Sprockets owns Blueprint package CSS. Per D-06, `styles.js` may load Gala overrides, typography, namespace bridging, and Blueprint JS helpers, but not Blueprint package CSS. Per D-07, any proven shared style regression should be fixed in the narrowest shared compatibility layer. Per D-08, preserve the current layout order unless browser QA proves a concrete ordering bug.

Output: Focused asset contract tests and any narrow fixes needed to keep the layout, Sprockets manifest, Shakapacker style entry, webpack config, and Shakapacker config aligned.
</objective>

<execution_context>
@/Users/nathanpapes/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nathanpapes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-asset-gate/01-CONTEXT.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@app/views/layouts/application.html.erb
@app/assets/stylesheets/application.css
@app/javascript/packs/styles.js
@config/webpack/environment.js
@config/shakapacker.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add asset ownership regression tests</name>
  <files>app/javascript/shared/__tests__/blueprintAssetContract.test.js, app/assets/stylesheets/application.css, app/javascript/packs/styles.js, app/views/layouts/application.html.erb</files>
  <behavior>
    - Test 1: `application.css` contains the Blueprint icon, core, datetime, popover2, and select package CSS requires.
    - Test 2: `styles.js` imports `shared/blueprint`, `shared/blueprintLegacyNamespace`, `shared/galaTypography`, and `FocusStyleManager`, but contains no `@blueprintjs/*/lib/css` imports.
    - Test 3: `application.html.erb` loads collected JavaScript pack `styles` before collected stylesheet pack `styles`, and loads Sprockets `application` stylesheet after the collected stylesheet pack.
  </behavior>
  <action>Create `app/javascript/shared/__tests__/blueprintAssetContract.test.js` as a Jest file that reads the three source files with Node `fs` and asserts the asset-loading contract. Use plain string/regex assertions rather than browser rendering. Implement exactly the locked CSS ownership decisions: Sprockets package CSS per D-05, no Blueprint package CSS in `styles.js` per D-06, and current layout order preserved per D-08. Do not add route-specific CSS shims and do not duplicate Blueprint CSS imports.</action>
  <verify>
    <automated>yarn test app/javascript/shared/__tests__/blueprintAssetContract.test.js --runInBand</automated>
  </verify>
  <done>The new Jest test passes and would fail if Blueprint package CSS is imported from `styles.js` or removed from `application.css`.</done>
</task>

<task type="auto">
  <name>Task 2: Fix only proven global asset contract violations</name>
  <files>app/views/layouts/application.html.erb, app/assets/stylesheets/application.css, app/javascript/packs/styles.js, config/webpack/environment.js, config/shakapacker.yml</files>
  <action>Run the new asset contract test. If it fails, make the narrowest correction needed to match D-05, D-06, D-07, and D-08. Preserve `app/javascript/packs/styles.js` as the entry for `shared/blueprint`, `shared/blueprintLegacyNamespace`, `shared/galaTypography`, and `FocusStyleManager.onlyShowFocusOnTabs()`. Preserve the Sprockets Blueprint package CSS requires in `application.css`. Check `config/webpack/environment.js` and `config/shakapacker.yml` only for contradictions that would cause CSS duplication or missing pack output; do not perform unrelated dependency or bundler upgrades.</action>
  <verify>
    <automated>yarn test app/javascript/shared/__tests__/blueprintAssetContract.test.js --runInBand</automated>
  </verify>
  <done>Global asset ownership is enforced by test, package CSS is loaded through Sprockets only, and no unrelated webpack/Shakapacker changes are introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Rails layout assets | Untrusted browsers request global scripts and styles emitted by Rails/Shakapacker/Sprockets. |
| Source tree -> browser bundle | Local source imports determine which code and CSS ship to every route. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | `app/javascript/packs/styles.js` | mitigate | Add Jest assertions that reject Blueprint package CSS imports in the Shakapacker style entry per D-06. |
| T-01-01-02 | Denial of Service | `application.html.erb` asset tags | mitigate | Preserve tested asset order so missing pack/CSS loads fail targeted checks before route QA. |
| T-01-01-03 | Information Disclosure | `config/shakapacker.yml` / webpack output | accept | No secrets or user data are introduced; this plan only validates asset ownership. |
</threat_model>

<verification>
Run `yarn test app/javascript/shared/__tests__/blueprintAssetContract.test.js --runInBand`.
</verification>

<success_criteria>
FOUND-02 is satisfied when the global application layout and asset entrypoints have a tested ownership contract: Sprockets loads Blueprint package CSS, Shakapacker loads Gala overrides and JS helpers, and the intentional layout asset order is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-asset-gate/01-baseline-asset-gate-01-SUMMARY.md`.
</output>
