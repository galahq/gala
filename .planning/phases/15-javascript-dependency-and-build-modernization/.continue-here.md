---
context: phase
phase: 15-javascript-dependency-and-build-modernization
task: 0
total_tasks: 0
status: paused
last_updated: 2026-05-12T04:16:42Z
---

<current_state>
Phase 14 is complete and committed. Phase 15 was just opened during `$gsd-autonomous`, but no Phase 15 plans, summaries, code changes, or lockfile changes were made before the pause request.

Current branch: `infra/sst-aws-poc`.

Latest commits:
- `2894dba8` docs(14): complete ruby dependency modernization
- `278c2acf` chore(14-02): update compatible test gems
- `1b807818` chore(14-01): update compatible runtime gems
- `2e7b8983` docs(14): plan ruby dependency modernization
</current_state>

<completed_work>

Completed Tasks:
- Phase 14 Plan 1: Runtime Ruby dependency update - Done, committed as `1b807818`.
- Phase 14 Plan 2: Development/test Ruby dependency update - Done, committed as `278c2acf`.
- Phase 14 completion docs/state - Done, committed as `2894dba8`.

Phase 14 verification:
- `docker compose exec web bundle check` passed for runtime and dev/test batches.
- `docker compose exec web bundle exec rails runner 'puts Rails.version'` passed and printed `8.1.3`.
- `docker compose exec web bundle exec rspec` passed after each dependency batch: 465 examples, 0 failures.
- `docker compose exec web bundle exec rake test:unit` passed: 465 examples, 0 failures.
- `docker compose exec web sh -lc 'SECRET_KEY_BASE=build-placeholder DATABASE_URL=postgresql://placeholder/placeholder bundle exec rails assets:precompile'` passed.
</completed_work>

<remaining_work>

- Phase 15 discuss/context artifacts: Not started.
- Phase 15 research/validation artifacts: Not started.
- Phase 15 executable plans: Not started.
- Phase 15 dependency updates: Not started.
- Phase 15 verification and commit sequence: Not started.
</remaining_work>

<decisions_made>

- Phase 14 held Ruby runtime at 4.0.3; Ruby 4.0.4 is a separate image/runtime upgrade.
- Phase 14 held Puma 8, Sidekiq 8, Devise 5, Sentry 6, RSpec Rails 8, Administrate 1, Selenium 4.43, and sqlite3 2.x for separate compatibility work.
- `spec/rails_helper.rb` now forces `RAILS_ENV=test` because the Docker web container exports `RAILS_ENV=development`, which caused request specs to run with development CSRF settings.
- Phase 15 should keep React and BlueprintJS major versions held during v1.1.
- Phase 15 should preserve Shakapacker/Webpack as the production bundler; Phase 12 did not accept Vite production replacement.
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)

1. `.planning/STATE.md` — current milestone position now points to Phase 15.
1. `.planning/ROADMAP.md` — Phase 15 goal, requirements, and success criteria.
1. `.planning/REQUIREMENTS.md` — JS-01 through JS-04 and QA expectations.
1. `.planning/phases/12-vitest-and-vite-feasibility/12-01-SUMMARY.md` — Vite/Shakapacker and Vitest/Jest decisions that constrain Phase 15.
1. `.planning/phases/14-ruby-dependency-modernization/14-VERIFICATION.md` — most recent completed phase gates and known warning noise.
1. `package.json` and `pnpm-lock.yaml` — current JavaScript dependency baseline.

## Critical Anti-Patterns (do NOT repeat these)

- Do not stage or commit the four pre-existing dirty frontend files unless the resumed work explicitly owns those changes.
- Do not upgrade React, BlueprintJS, Shakapacker/Vite production integration, or Jest/Vitest runner behavior as incidental dependency churn in Phase 15.
- Do not change package dependencies before creating Phase 15 research, validation, and plan artifacts.

## Infrastructure State

- Docker services were available during Phase 14 verification.
- No dev server or long-running watcher was intentionally left running by this pause.
- Known warning noise remains: Bundler/RubyGems platform constant redefinition warnings and Rails 8.2 deprecation warnings.
- `git status --short` at pause showed only the four pre-existing dirty frontend files:
  - `app/javascript/catalog/search_results/NoSearchResults.jsx`
  - `app/javascript/shared/blueprint.scss`
  - `app/javascript/shared/blueprintLegacyNamespace.js`
  - `app/javascript/stats/__tests__/DatePicker.test.jsx`

<context>
Phase 15 discovery had just begun. Useful early facts:
- `pnpm outdated --format json` was run and showed many packages with latest major versions that should be held due React 16, BlueprintJS 4.x, Jest 24/Vitest migration, or Shakapacker/Webpack constraints.
- Small compatible candidates include patch/minor updates around Babel, Sass, Webpack 5 patch, `whatwg-fetch`, `prop-types`, and selected loader/build utilities, but these still need Phase 15 research and plan artifacts before execution.
- Conservative hold candidates include React/ReactDOM 19, BlueprintJS 6, Jest 30, jsdom 29, Vite plugin 6, CSS loader 7, Webpack CLI 7, Webpack merge 6, major React ecosystem libraries, and deprecated library replacements.
</context>

<next_action>
Start with: create Phase 15 context/research/validation/plans from `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, Phase 12 summary, `package.json`, and current `pnpm outdated --format json` results. Then run small pnpm update batches with `pnpm install --frozen-lockfile`, `pnpm test -- --runInBand`, and Docker assets precompile gates before committing.
</next_action>
