---
phase: 30
plan: 30-01
title: Audit dirty cache/deploy artifacts and validate guardrails
status: complete
created: 2026-06-01
completed: 2026-06-01
wave: 1
---

# Plan 30-01

## Goal

Close the remaining dirty cache/deploy artifacts by auditing the worktree, validating the focused cache and deployment guardrails, updating durable GSD state, and committing the closeout without broad unrelated rewrites.

## Steps

1. Audit dirty files with `git status` and `git diff`.
2. Normalize trivial file drift without changing app behavior.
3. Reconcile `.planning/ROADMAP.md` phase checkboxes against completed summaries on disk.
4. Add Phase 30 closeout documentation and a milestone audit note that records remaining CI validation debt.
5. Validate focused cache behavior with `./run-rspec.sh spec/requests/catalog_routes_spec.rb spec/requests/case_routes_spec.rb`.
6. Validate local workflow/syntax guardrails with YAML parsing, shell syntax checks, operator doc validation, CI script tests, and TypeScript no-emit checks.
7. Inspect GitHub workflow runs and CI validation artifacts with `gh`.
8. Inspect SST-managed AWS runtime health with read-only `AWS_PROFILE=gala AWS_REGION=us-west-2` commands and public `/up` checks.
9. Commit the scoped closeout artifacts.

## Gates

- Targeted cache request specs must pass.
- Static workflow/operator checks must pass.
- GitHub validation artifacts must be inspected directly.
- AWS inspection must be read-only and must not touch Heroku, `.com` DNS, SES, or retained media buckets.
- Any remaining broad CI/lint debt must be recorded instead of hidden.
