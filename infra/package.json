{
  "name": "gala-infra",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "deploy": "sst deploy",
    "dev": "sst dev",
    "diff": "sst diff",
    "remove": "sst remove"
  },
  "devDependencies": {
    "sst": "4.7.1",
    "typescript": "^6.0.2"
  }
}
